apiVersion: v1
kind: Service
metadata:
  labels:
    app: mnode
  name: mnode
spec:
  clusterIP: None
  ports:
  - name: 80-22
    port: 80
    protocol: TCP
    targetPort: 22
  selector:
    app: mnode
  type: ClusterIP
